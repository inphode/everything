[user]
        email = phillquantrill@gmail.com
        name = Phill

[core]
	excludesfile = ~/.gitignore-global

[alias]
    dt = "!f() { nvim -p $(git diff --name-only $@) +\"tabdo Gdiff $@\" +tabfirst; }; f"

